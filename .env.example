PORT=4000
MONGODB_URI=mongodb+srv://usuario:password@cluster0.xxxxx.mongodb.net/oklahoma?retryWrites=true&w=majority
CORS_ORIGIN=http://localhost:5173

# Mercado Pago (panel de desarrolladores > credenciales de produccion)
MP_ACCESS_TOKEN=

# URLs publicas (para back_urls y el webhook de Mercado Pago).
# En desarrollo local dejalas como estan; el webhook solo funciona con una
# URL publica real, asi que se prueba de verdad una vez desplegado.
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:4000
